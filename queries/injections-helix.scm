; Datastar injections are host-language queries, not Datastar-language queries.
;
; This file is intentionally query-empty so `tree-sitter test` can validate the
; Datastar query set against the Datastar grammar.
;
; For Helix HTML injection setup, use:
; docs/helix-html-injections.scm
